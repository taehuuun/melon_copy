import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const mainScreen = () => {
  return <SafeAreaView></SafeAreaView>;
};

export default mainScreen;
